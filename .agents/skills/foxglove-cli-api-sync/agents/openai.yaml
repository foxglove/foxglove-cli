interface:
  display_name: "Foxglove CLI API Sync"
  short_description: "Sync requested CLI features with the public API"
  default_prompt: "Use $foxglove-cli-api-sync to update the CLI commands or features I request against the public API spec."
